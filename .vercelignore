# We do NOT exclude data/ since `lib/local-data.ts` imports the Practo JSON dataset
# at build time so the site works without DATABASE_URL.
scraper/
scripts/
supabase/
hanuonepro/
.git/
.next/
node_modules/
